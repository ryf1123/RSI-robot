# RSI-robot：用 agent 迭代机器人策略，agent 到底贡献了什么

在 Mac mini（M4，16 GB，无 CUDA）上，把「LLM agent 自动迭代机器人设计」这条链路自己搭一遍，
并且把它做成**可证伪**的：不是问「agent 有没有用」，而是问**「agent 用的是哪一部分信息」**。

## 靶心问题

Eureka 这一路的范式是：LLM 写奖励函数 → RL 训练 → 把训练统计喂回 LLM → LLM 改 → 循环。
论文报告它在 83% 的任务上超过人类专家。但这个结论里混着至少四样东西：

1. **先验**：LLM 本来就知道「hopper 要往前走、要站直、要省力」；
2. **反馈**：它读到了这一轮的训练统计；
3. **选择压力**：外层循环保留最好的，随机提议 + 精英保留本身就会变好；
4. **搜索空间**：人写的搜索空间已经把大部分坏解排除掉了。

把这四样拆开量，就是这个项目。

## 怎么做成可证伪的

搜索空间是**故意造出来的**：17 个奖励项里，有 5 个是我埋的诱饵——

- `back_vel` / `stand_still` / `foot_press`：**有害**，正权重会主动把任务做坏；
- `y_drift` / `clock`：**无关**，在这个平面模型上恒等于 0 或与任务无关。

于是「诱饵权重占比」（decoy mass）是一个**上下限都已知**的指标：
理想 0.0，均匀随机 5/17 = **0.294**。这正是本项目的主诊断量——
它测的不是「找到多好的解」，而是「提议者到底懂不懂它在改什么」。

适应度用的是**任务指标**（3 秒内前进的米数），不是被设计的奖励——设计者改不动它。

## 分环

| 环 | 内容 |
|-|-|
| 0 | 环境、内层优化器（ARS）、17 项奖励库、设计空间、评测与诊断 |
| 1 | 闭环：agent 提议 → 训练 → 反馈 → 再提议，能不能超过手写奖励 |
| 2 | **靶心：2×2 信息消融**（命名/匿名 × 有反馈/无反馈）+ 随机下限 + 精英保留对照 |
| 3 | 提议 vs 选择：把「选择压力」这一项单独减掉 |
| 4 | 反馈的粒度：只给标量 fitness vs 给完整诊断 vs 给错配的诊断 |
| 5 | 跨任务迁移：A 任务上进化出的设计搬到 B 任务 |
| 6 | 成本曲线与失败模式 |

## 预注册（先写下来，再跑）

见 `notes/00_preregistration.md`。
